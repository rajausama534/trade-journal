"use client";
import { FormEvent,useState } from "react"; import { Activity } from "lucide-react"; import { supabase } from "@/lib/supabase";
export function AuthScreen(){const [signup,setSignup]=useState(false),[busy,setBusy]=useState(false),[message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMessage("");const d=new FormData(e.currentTarget),email=String(d.get("email")),password=String(d.get("password"));if(!supabase)return;const {error}=signup?await supabase.auth.signUp({email,password}):await supabase.auth.signInWithPassword({email,password});setBusy(false);if(error)setMessage(error.message);else if(signup)setMessage("Check your email to confirm your account, then sign in.")}return <main className="auth"><section><div className="auth-brand"><Activity/>Trade<span>Flow</span></div><h1>{signup?"Create your journal":"Welcome back"}</h1><p>{signup?"Start tracking every trade securely.":"Sign in to view your private trading data."}</p><form onSubmit={submit}><label>Email<input name="email" type="email" required placeholder="you@example.com"/></label><label>Password<input name="password" type="password" minLength={6} required placeholder="At least 6 characters"/></label>{message&&<div className="auth-message">{message}</div>}<button className="primary" disabled={busy}>{busy?"Please wait…":signup?"Create account":"Sign in"}</button></form><button className="auth-switch" onClick={()=>{setSignup(!signup);setMessage("")}}>{signup?"Already registered? Sign in":"New here? Create an account"}</button></section></main>}
